component:
  name: evaluation
  description: ''
  provider: fate
  version: 2.0.0-alpha
  labels: []
  roles:
    - guest
    - host
    - arbiter
  input_definitions:
    parameters:
      eval_type:
        type: parameter_eval_type
        default: binary
        optional: true
        description: ''
        type_meta:
          title: parameter_eval_type
          type: object
          properties:
            data:
              title: Data
              default: binary
              type: string
    artifacts:
      input_data:
        type: dataset
        optional: false
        stages:
          - default
        roles:
          - guest
          - host
          - arbiter
        description: ''
  output_definitions:
    artifacts:
      output_metric:
        type: classification_metrics
        optional: false
        stages:
          - default
        roles:
          - guest
          - host
          - arbiter
        description: ''
schema_version: v1

